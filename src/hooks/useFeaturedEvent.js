import {useEffect,useState} from 'react';
import {api} from '../lib/api';
const empty={date:'Date to be confirmed',event:'Society events',venue:'See fixtures for details',cost:'To be confirmed',meetTime:'To be confirmed',teeTime:'To be confirmed',deadline:'To be confirmed'};
export default function useFeaturedEvent(charityOnly=false){const [event,setEvent]=useState(empty);useEffect(()=>{api.get('/api/public-fixtures').then(({events})=>{const candidates=charityOnly?events.filter(e=>e.bookingFields.isCharity || /charity/i.test(e.title)):events;const today=new Date().toISOString().slice(0,10);const e=candidates.find(e=>e.event_date>=today&&e.status!=='completed') || candidates.at(-1);if(!e)return;const format=d=>new Date(d+'T12:00:00Z').toLocaleDateString('en-GB',{day:'2-digit',month:'short',year:'numeric',timeZone:'UTC'});setEvent({id:e.id,date:format(e.event_date),event:e.title,venue:e.venue,cost:e.cost?`£${String(e.cost).replace(/^£/,'')}`:'To be confirmed',meetTime:e.meet_time,teeTime:e.tee_time,deadline:e.registration_closes_at?new Date(e.registration_closes_at).toLocaleDateString('en-GB',{timeZone:'Europe/London'}):'To be confirmed',capacity:e.bookingFields.capacity,package:e.bookingFields.package,schedule:e.bookingFields.schedule});}).catch(()=>setEvent(empty));},[charityOnly]);return event;}
